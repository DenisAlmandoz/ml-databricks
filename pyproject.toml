[project]
name = "mlops-project"
version = "0.1.0"
description = "Simple MLOps fundamentals project for Databricks-style workflows"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
mlops-train = "mlops_project.training.train:main"
mlops-batch-inference = "mlops_project.serving.batch_inference:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
