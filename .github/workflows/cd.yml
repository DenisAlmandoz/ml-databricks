name: cd
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v3
      - run: echo "Deploy Databricks bundle, Terraform, or Kubernetes manifests here."
